// No appsetings é onde colocamos as configurações dos bancos de dados email e outras informações.
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  //Strings de conexão podem ser achadas na documentação da Microsoft. Elas são as informações necessárias
  //par se conectar ao servidor. No caso abaixo é servidor de banco de dados LocalDb do prórpio SQL Server local da máquina.
  // Ao instalar o Visual Studio , já é disponibilizado esse banco de dados. Se for um banco de dados remoto (fora da máquina)
  // será inserido a especificação do banco de dado no "Server=", o serviço de banco de dados remoto dará as informações necessárias
  //"Database=" ´o nome do banco de dados e o resto é o tipo de conexão. Há varios tipos de conexões, devemos usar a que melhor 
  // supri as necessidades
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=mf-dev-backend;Trusted_Connection=True;MultipleActiveResultSets=true"
  }
}
